version: '3.8'

services:
  frontend:
    build:
      context: .
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL}
    container_name: pejuang_mimpi_frontend
    ports:
      - "8080:80" # Map port 8080 on your host to port 80 in the container
    restart: unless-stopped